---
## PLAYBOOK TO INSTALL AND CONFIGURE MYSQL DB ON REDHAT/ DEBIAN SYSTEMS

    - hosts: all
       vars_prompt:
        - name: "MySQLPass"
          prompt: "Enter MySQL root password"
          private: yes
          default: "P@$$w0rd"

       roles:
        - mysql



